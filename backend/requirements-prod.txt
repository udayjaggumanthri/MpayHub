# mPayHub — production runtime dependencies
# Install on VPS / Gunicorn hosts:
#   pip install -r requirements-prod.txt
#
# System packages (not pip):
#   - PostgreSQL client libraries (psycopg2-binary wheels usually suffice)
#   - redis-server when REDIS_URL is set in .env (required for login/session/cache)

# Core Django + API
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
drf-spectacular>=0.26.0

# Database
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# Security, auth helpers, CORS, rate limiting
django-cors-headers>=4.3.0
django-ratelimit>=4.1.0
cryptography>=41.0.0

# HTTP clients (BBPS, Razorpay, Cashfree, SMS, GeoIP HTTP provider)
requests>=2.31.0

# GeoIP (optional: used when GEOIP_PROVIDER=maxmind; default is http)
geoip2>=4.8.0
maxminddb>=2.2.0

# Excel exports (QR ops, wallet adjustments, BBPS MDM import)
openpyxl>=3.1.0

# Cache — REQUIRED when REDIS_URL is set (.env / production settings)
redis>=4.6.0

# Config + media
python-decouple>=3.8
Pillow>=10.0.0

# Production WSGI + static files (collectstatic)
gunicorn>=21.2.0
whitenoise>=6.6.0
