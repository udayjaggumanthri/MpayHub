# =============================================================================
# mPayHub — production / UAT runtime dependencies (Python)
# =============================================================================
# Install on VPS / Gunicorn hosts (after activating venv):
#   pip install --upgrade pip
#   pip install -r requirements-prod.txt
#
# Local development with tests and lint tools:
#   pip install -r requirements-dev.txt
#   # or: pip install -r requirements.txt   (alias of requirements-dev)
#
# System packages (not installed by pip):
#   - Python 3.10+ (3.11 recommended)
#   - PostgreSQL 14+ client/server (or set USE_SQLITE=True for local-only smoke)
#   - redis-server when REDIS_URL is set (login, sessions, cache, rate limits)
#
# Frontend dependencies are NOT here — see ../frontend/package.json (npm).
# Load-test tooling: see ../loadtest/requirements.txt (Locust).
# =============================================================================

# Core Django + API
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0
drf-spectacular>=0.26.0

# Database
psycopg2-binary>=2.9.9
dj-database-url>=2.1.0

# Security, auth helpers, CORS, rate limiting
django-cors-headers>=4.3.0
django-ratelimit>=4.1.0
cryptography>=41.0.0

# HTTP clients (BBPS, Razorpay, Cashfree, SMS, GeoIP HTTP provider)
requests>=2.31.0

# GeoIP (optional: used when GEOIP_PROVIDER=maxmind; default is http)
geoip2>=4.8.0
maxminddb>=2.2.0

# Excel exports (QR ops, wallet adjustments, BBPS MDM import)
openpyxl>=3.1.0

# Cache — REQUIRED when REDIS_URL is set (.env / production settings)
redis>=4.6.0

# Config + media
python-decouple>=3.8
Pillow>=10.0.0
# Optional S3 media (USE_S3=True); local FileSystemStorage when False
django-storages>=1.14.0
boto3>=1.34.0

# Production WSGI + static files (collectstatic)
gunicorn>=21.2.0
whitenoise>=6.6.0
