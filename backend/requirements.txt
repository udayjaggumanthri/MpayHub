# mPayHub backend dependencies
#
# Production VPS (Gunicorn + PostgreSQL + Redis):
#   pip install -r requirements-prod.txt
#
# Local development (includes tests, lint, debug toolbar):
#   pip install -r requirements-dev.txt
#
# This file keeps backward compatibility — same as requirements-dev.txt:
-r requirements-dev.txt
